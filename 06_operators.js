
/************ operators **********
 
 * Arithmetic (+ - * / % ++ --)
 * Assignment (= += -= *= /= %=)
 * Comparison (== === != !== > >= < <=)
 * Logical (|| && !)
 * Bitwise (| & ~ ^ << >>)
 * Type (typeof())
 * Ternary Operators
 * String Operators
 
 *********************************/

// == check only values. type doesn't matter
// === type and values both are check

// =! check only values. type doesn't matter
// ==! type and values both are check

// ~ binary not
// ^ binary xor
// << left shift
// >> right shift
